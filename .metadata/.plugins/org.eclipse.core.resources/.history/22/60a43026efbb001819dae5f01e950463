<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml"
 xmlns:th="http://www.thymeleaf.org">
<head>
 <title>Home</title>
 <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
 <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
 <script src="https://code.jquery.com/jquery-1.11.1.min.js"></script>
 <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.0.0/css/font-awesome.min.css">
</head>
<body>
 <div class="container">
  <h2 align="center">Hi <span th:utext="${userName}"></span>, welcome back to BlogCity.</h2>
   
   <h3 align="center">Read some Blogs</h3>
   <div>
   <table align="center">
  	<tbody>
    <tr th:if="${blogs.empty}">
            <td colspan="2"> No blogs Available </td>
        </tr>
        
        <tr th:each="blog : ${blogs}">
              <td th:text="${blog.title}" />
              <td th:text="${blog.author_id}" />
             <td><a th:href="@{'/blog/' + ${blog.id}}">view</a></td> 
        </tr>
    </tbody>
        </table>
<!--         <table align="right">
    
      	<tbody>
    
            <tr th:each="name : ${authorNames}">
             <td th:text="${name}" />
        </tr>
            </tbody>
        
    </table> -->
    </div>
  <a th:href="@{/blog/create}">Write a Blog</a> 
  <Br/>
  <a th:href="@{/logout}">Sign out</a>
 </div>
</body>
</html>